# Frontend (apps/web) — Vite. Alleen VITE_*-variabelen komen in de browser.

# Backend-API. Inloggen loopt via deze API (/api/login) die met de metro-backend
# praat; de metro-URL staat daarom alleen in apps/api/.env, niet hier.
VITE_API_URL=http://localhost:4000
